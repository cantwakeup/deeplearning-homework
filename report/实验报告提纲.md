# 深度学习课程实验报告提纲

> 最终提交要求是 Word 版本；本文件用于先整理内容。

## 封面

- 题目：深度学习课程实验
- 学生姓名：
- 班级学号：
- 指导教师：
- 设计时间：

## 开发环境描述

- 操作系统：
- Python 版本：
- NumPy 版本：
- PyTorch 版本：
- torchvision 版本：
- 开发工具：

## 第一章 NumPy 实现 MLP

### 原题目

编写 MLP，只能使用 NumPy 编写，不能直接使用 PyTorch 中的 linear、sigmoid 等函数；完成输入层、隐含层和输出层；编写损失函数、激活函数、训练过程和预测过程；回归或分类均可，数据集可自行选择。

### 题目分析及解题思路

说明数据集、网络结构、前向传播、反向传播、损失函数和评价指标。

### 实验过程

结合 `01_numpy_mlp/` 中的代码说明编码过程。

### 实验结果及分析

当前结果：训练 1500 个 epoch，最终损失为 0.1603，训练集准确率为 0.9750，测试集准确率为 0.9667。结果文件中保留了 10 个测试样本的真实类别和预测类别。模型能够较好地区分三分类螺旋数据，说明手写前向传播、交叉熵损失和反向传播流程有效。

## 第二章 PyTorch CNN 手写数字识别

### 原题目

编写一个卷积网络，对手写体数字进行识别。数据集使用 torch 中自带的样本数据集；卷积网络结构、激活函数、损失函数和超参数自定义；保存训练结果；分析多种情况并保留分析结果。

### 题目分析及解题思路

说明 MNIST 数据集、CNN 结构、训练策略和评价指标。

### 实验过程

结合 `02_mnist_cnn/` 中的代码说明编码过程。

### 实验结果及分析

当前结果：在 GPU 上训练 3 个 epoch，batch size 为 256，最终训练集准确率为 0.9841，测试集准确率为 0.9900。随着训练轮数增加，测试损失从 0.0465 下降到 0.0295；`cnn_mnist_epoch_comparison.csv` 保留了 1、2、3 个 epoch 三种情况的结果。结果文件中也保留了 10 个测试样本的预测样例，说明 CNN 对 MNIST 图像特征提取有效。

## 第三章 PyTorch 实现 ResNet

### 原题目

使用 PyTorch 实现 ResNet，层数不限，建议 50 层，数据集可以使用 MNIST，完成训练和预测。

### 题目分析及解题思路

说明残差连接、Bottleneck 模块、ResNet-50 结构和 MNIST 输入适配方式。

### 实验过程

结合 `03_resnet_mnist/` 中的代码说明编码过程。

### 实验结果及分析

当前结果：在 GPU 上训练 ResNet-50 共 3 个 epoch，batch size 为 256，最终训练集准确率为 0.9898，测试集准确率为 0.9872。结果文件中保留了 10 个测试样本的预测样例。ResNet-50 参数量较大，在 MNIST 这类简单数据集上可以取得较高准确率，但相比轻量 CNN 不一定更有优势。

## 总结

总结三类网络的实现差异、训练效果和收获。

## 参考文献

[1] LeCun Y, Bottou L, Bengio Y, Haffner P. Gradient-based learning applied to document recognition. Proceedings of the IEEE, 1998.

[2] He K, Zhang X, Ren S, Sun J. Deep Residual Learning for Image Recognition. CVPR, 2016.
